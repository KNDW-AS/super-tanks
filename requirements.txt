# Aeris Gateway Dependencies
# Pinned to versions installed on Z620 — 2026-03-15

# ── Core runtime (required for main_loop) ──────────────────────
chromadb==1.5.1
requests==2.32.5
psutil==7.2.2
python-dotenv==1.2.1
PyYAML==6.0.3
aiohttp==3.13.3
python-telegram-bot==22.6
google-genai==1.64.0
openai==2.21.0
numpy==2.4.2
python-dateutil==2.9.0.post0

# ── Skills & modules ───────────────────────────────────────────
paho-mqtt==2.1.0
duckduckgo-search==8.1.1
playwright==1.58.0
beautifulsoup4==4.14.3
markdownify==1.2.2
cryptography==46.0.5
watchdog==6.0.0

# ── Voice pipeline (optional — Z620 CPU or Legion GPU) ────────
faster-whisper==1.2.1
sounddevice==0.5.5
torch==2.10.0

# ── Dev / test ─────────────────────────────────────────────────
pytest==9.0.2
pytest-asyncio==1.3.0
pytest-timeout==2.4.0
pytest-cov==7.1.0
